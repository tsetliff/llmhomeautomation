#!/usr/bin/bash

echo "Got this far!"

# Check if the virtual environment is activated
if [[ -z "$VIRTUAL_ENV" ]]; then
    echo "Activating virtual environment..."
    source venv/bin/activate
fi

# Run the ask.py script
python ask.py
